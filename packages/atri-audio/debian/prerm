#!/bin/sh -e

case "$1" in
    remove|purge)
        systemctl stop atrivolume.service 2>/dev/null || true
        systemctl disable atrivolume.service 2>/dev/null || true
        ;;
    upgrade|deconfigure)
        ;;
    *)
        ;;
esac

#DEBHELPER#
exit 0
