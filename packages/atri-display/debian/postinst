#!/bin/sh -e

case "$1" in
    configure)
        systemctl --no-reload enable atri-displayd.service 2>/dev/null || true
        ;;
    abort-upgrade|abort-remove|abort-deconfigure)
        ;;
    *)
        ;;
esac

#DEBHELPER#
exit 0
